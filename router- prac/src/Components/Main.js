import styled from 'styled-components';

const MainBox = styled.div`
  width : 100vw;
  height: 100vh;
  display: flex;
  justify-content: center;

`

const Main = () => {
  return(
    <MainBox>
    </MainBox>
  )
}

export default Main;